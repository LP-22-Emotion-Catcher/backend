from database.models import Wall, Post


def get_wall(wall):
    pass
    #wall = Wall.query.filter(Wall.name == wall).first()
    #return wall


def get_post(post):
    pass
    #post = Post.query.filter(Post.name == post).first()
    #return post
